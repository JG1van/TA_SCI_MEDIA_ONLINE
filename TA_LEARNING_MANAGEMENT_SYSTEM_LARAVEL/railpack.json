{
  "php": {
    "extensions": ["gd", "exif", "pdo", "pdo_mysql", "mbstring", "tokenizer", "xml", "ctype", "fileinfo", "bcmath", "curl", "zip", "redis"]
  },
  "deploy": {
    "startCommand": "php artisan migrate:status --force 2>/dev/null; php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
